import 'package:flutter/material.dart';

class userWardrobeList extends StatelessWidget {
  const userWardrobeList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body : Center(
          child: Text("여기는 옷장 입니다"),
        )
    );
  }
}
